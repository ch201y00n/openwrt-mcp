# Native Windows validation

Updated: 2026-09-14 (Asia/Seoul). This is local Windows GNU host/fixture acceptance, not WSL, MSVC, macOS, OpenWrt deployment or physical-router acceptance. Original commit IDs in historical entries map to equivalent source trees in [history normalization](history-normalization.md).

## Executed gate and scope

The **v19 private-log gate** passed with **568 distinct native tests**, zero ignored
and **107 harness regressions** counted once. The architecture-only checkpoint
`f3059d3` (formerly `15dae37`) passed 551 native tests before implementation;
Linux-on-WSL separately passes 572. Native host-platform now runs twenty file cases
and four pure policy cases; three new server cases include actual executable MCP
file rotation, safe JSON/text rendering, disabled logging and terminal failure.
Native release: **4,413,952 bytes**, SHA-256
`ef35618f6ffcc2a5a8723c6df96415dfdc515771f8ae6de0ea8fbe25fd7f8b73`.
Architecture/evolution, formatting, strict all-target Clippy, workspace tests and
release compilation pass. No SDK feature/dependency/download change. Exact native
rename and ancestor-sharing defects were reproduced and fixed; [profile evidence](windows-private-logs.md)
records the scope without raw production diagnostics or fallback. These are private
synthetic NTFS fixtures, not real audit storage, Vault, durable logs or new
macOS/MSVC/other-Windows/physical-router acceptance. The unchanged Linux artifact
and remaining gaps are recorded in [validation](validation.md).

The **v18 supplied-stream sealing gate** passed with **546 distinct native tests**, zero ignored and **102 harness regressions** counted once. Architecture-only `47cf2e40e8f82bd28eabe9ffa16018369f0c1443` passed 515 native tests before behavior. Linux-on-WSL separately passes 567 implementation tests. Native server: **4,400,128 bytes**, SHA-256 `1cec67125538c3c458759de78afd079d4114d26f465cfaeb2527aeee29c3ac5e`. Full structure/evolution, formatting, all-target Clippy, tests and release compilation pass. [Actual age/gzip composition](validated-archive-sealing.md) uses only synthetic memory fixtures; private/atomic/durable stage declarations are not OS proof. No real capture/store, mutation or macOS/MSVC acceptance follows. During the prior architecture gate, missing files in the disposable Cargo source cache were restored offline from 267 checksum-verified cached archives by invalidating only completion markers, not deleting repositories or user data.

The **v17 gzip archive gate** passed with **510 distinct native tests**, zero ignored and **97 harness regressions** counted once. Architecture-only `e72e685361e86f90516d3985c06ef749ce124bb2` preceded behavior; Linux-on-WSL separately passed 531 tests. Native server: **4,400,128 bytes**, SHA-256 `960462bb1f2c3d8793dfb2262095c8ef40672c80d740dbd0452f5c358a283514`. The full gate includes all-target Clippy, tests and release compilation. [Independent gzip fixtures](gzip-archive-validation.md) record accepted unpadded producers and intentionally rejected direct bsdtar stdout padding. No actual backup, macOS/MSVC or memory/latency claim follows; later documentation-only changes preserve this production scope.

The **v16 supplied archive-codec gate** passed at **2026-09-13T13:16:12Z–13:18:04Z**, with **490 distinct native tests**, zero ignored and **92 harness regressions** counted once. Architecture-only `6c3e2564459ab78fce7ff418c07bebf6062d2631` preceded behavior. Full architecture/evolution, format, all-target Clippy, tests and release compilation passed; Linux-on-WSL separately passed 511 tests. Native server is **4,400,128 bytes**, SHA-256 `81d61f40f5407c8ed78ab6d41a6232168eb2d38f49e0edd4ccf39c60d0c777f0`. [Independent host tar fixtures](backup-archive-validation.md) also pass, but do not establish OpenWrt backup/restore or a production consumer. No macOS/MSVC or new resource measurement follows. Later documentation-only changes preserve this production scope.

The **v15 optimized pure effect-model gate** passed at **2026-09-13T12:40:59Z–12:43:09Z**, with **470 distinct native tests**, zero ignored and **87 harness regressions** counted once. Source is architecture-only `8adc3278df83289bff7529fb483c90edbbe1db49` plus the isolated pure graph library, fifteen behavior tests and synthetic microbenchmark. Architecture/evolution, formatting, strict all-target Clippy, workspace tests and release compilation passed; Linux-on-WSL separately passed 491 tests. Native server remains **4,400,128 bytes**, SHA-256 `ebee6adcae425006711e08746bba1eb44549f6a0d15e59b3ddbb573249d2c43c`. The graph model has no production caller or device authority. [Synthetic timing evidence](protected-resource-effects.md) is not MCP/device latency, peak memory or native macOS/MSVC acceptance. Later evidence-only edits preserve this production scope.

The **v14 opkg root-status gate** passed at **2026-09-13T12:00:42Z–12:02:38Z**, with **450 distinct native tests**, zero ignored and **82 harness regressions** counted once. Source is architecture-only `2233f3e83a63313e98bcf9413c3efa78ea6ee042` plus the fixed opkg recipe, bounded parser, closed category operation and shared profile-bound package snapshot. Architecture/evolution, formatting, strict all-target Clippy, workspace tests and release compilation passed; Linux-on-WSL separately passed 471 distinct tests. Native release: **4,400,128 bytes**, SHA-256 `2441527d8bad1890351db35354f46ae7cc65f081618911e22cf7c41878c8cc16`. Native SSH tests use synthetic peers, not Windows-to-router acceptance. MSVC/macOS, full management and new RSS/CPU/latency remain unverified. Evidence-only edits do not change production source scope.

The **v13 base-service UCI gate** passed at **2026-09-13T11:11:54Z–11:13:53Z**, with **433 distinct native tests**, zero ignored and **79 harness regressions** counted once. Source is architecture-only `fe677cb2674b900972b1a1b219cc4bb6e8d9013c` plus eighteen new closed UCI views and independent contract/documentation/category/MCP regressions. Architecture/evolution, formatting, strict all-target Clippy, workspace tests and release compilation passed. Linux-on-WSL separately passed 454 distinct tests. Native release: **4,372,480 bytes**, SHA-256 `5f0f9b50fe9c864f2a4bdb64bec4e9763a489c40433ab3bb6b08af31086e27b7`. This is host-fixture evidence, not Windows-to-router, MSVC/macOS or full-management acceptance. No new RSS/CPU/latency measurement; later evidence-only edits do not change production source scope.

The **v12 bounded text-option gate** passed at **2026-09-13T10:39:08Z–10:40:42Z**, with **426 distinct native tests**, zero ignored and **75 harness regressions** counted once. Source is architecture-only `6c45623914ef327d6e8e007f8644e22905eda620` plus terminal nested TextOption and two v2 UCI consumers. Architecture/evolution, format, strict all-target Clippy, workspace tests and release compilation passed. Linux-on-WSL separately passed 447 distinct tests. Native release: **4,360,192 bytes**, SHA-256 `7fe6f23d09adabdaf503875e5a5d8d5fc8e9c86481b5230a112a3a27a8cfa8d0`. This is host/fixture evidence, not Windows-to-router, MSVC/macOS, physical OpenWrt or full-management acceptance. No new RSS/CPU/latency measurement; later documentation-only edits do not change this artifact's source scope.

The **v11 closed UCI gate** passed at **2026-09-13T10:14:48Z–10:16:11Z**, with **410 distinct native tests**, zero ignored and **70 harness regressions** counted once. Architecture-only `0b581eaceedfe81a8fd214b5df6875cc9633ccce` passed Windows and Linux gates before production changes; this run covers that checkpoint plus six closed UCI reads, exact text enums and schema-3 probe migration. Architecture/evolution, format, strict all-target Clippy, workspace tests and release compilation passed. Linux-on-WSL separately passed 431 distinct tests. Native release: **4,340,224 bytes**, SHA-256 `7f3d139389deaba740a8146343acbd033ed35fedb7990aaa2bd85df280b77f42`. This remains host/fixture evidence, not actual UCI configuration on a physical router, Windows-to-router, macOS/MSVC, Vault or full-management acceptance. No new RSS/CPU/latency measurement. Subsequent documentation-only changes do not change this binary's source scope.

The **interface-IP increment within v10** passed at **2026-09-13T09:36:40Z–09:37:48Z**, with **396 distinct native tests**, zero ignored and **65 harness regressions** counted once. Source is `e071f3e8ff8a8d39873f1a4f13fa86f1d936c38c` plus four typed interface observations and the Cargo-root harness correction described in validation.md. Architecture/evolution, format, strict all-target Clippy, workspace tests and release compilation passed. Linux-on-WSL separately passed 417 distinct tests. Native release: **4,321,280 bytes**, SHA-256 `0452feea5a574a9fe851c3695159515da3126b94a706a998fc071e9be751100a`. These remain local host/fixture results, not Windows-to-router, MSVC/macOS or full-management acceptance. No new RSS/CPU/latency claim.

The **v10 finite observation/DHCP gate** passed at **2026-09-13T09:22:49Z–09:24:01Z**, with **389 distinct native tests**, zero ignored and **63 harness regressions** counted once. Architecture-only `4040ce37ea4b09770ff268b038bcdcb810eeb42d` passed both host gates before implementation. Source for this run is that checkpoint plus finite rows/sentinels/error guards and two DHCP reads. Architecture/evolution, format, strict all-target Clippy, workspace tests and release compilation passed; Linux-on-WSL separately passed 410 distinct tests. Native release: **4,309,504 bytes**, SHA-256 `01ea1177901ee7a18370ae491cb7276ba6cc6f41a0572ed3a988171989eeb4cc`. This is a local fixture-tested artifact, not a publication or router acceptance. No current RSS/CPU/latency, Windows file-log or Vault acceptance follows. Documentation-only edits after this run do not change the binary's source scope.

The **v9 LuCI/storage gate** passed at **2026-09-13T09:01:00Z–09:02:11Z**, with **377 distinct native tests**, zero ignored and **61 harness regressions** counted once. Architecture-only `acf65324adad7b509b714e8deba8ea199993f5e9` passed both native Windows GNU and Linux-on-WSL gates before production migration. Source for this implementation run is that checkpoint plus the closed nine-object registry and two typed storage reads. Format, strict all-target Clippy, workspace tests and release compilation passed; a separate Linux-on-WSL gate passed 398 distinct tests. This remains host/fixture evidence, not Windows-to-router or emulated storage acceptance. The local Windows release is 4,280,832 bytes; earlier artifact hashes below remain tied to their original versions. No new RSS/CPU/latency measurement was made.

The **v8 protected-read increment** follows architecture-only `9c0117f582115fceff0d127374a7c55d7c4fcc49`, validated on native Windows GNU and Linux-on-WSL before behavior. It adds seven native host-platform file tests, three supplied-security-byte/path policy tests and one native file/ZIP key-source composition test. The v8 harness has 59 regressions counted once. The first complete implementation gate passed 370 distinct native tests before the additional ZIP composition case; the final inventory is recorded in validation.md. Tests create private synthetic folders directly below a trusted user profile and remove only their exact fixtures; no existing parent ACL, drive mapping, Vault or real key is changed. Native short aliases are tested when provided by NTFS, without changing volume settings. Windows private rotating logs/system logs and Vault remain unsupported; native read availability is a separate capability.

Final v8 evidence gate: **2026-09-13T08:36:36Z–08:37:32Z**, **371 distinct native tests**, zero ignored, architecture/evolution, format, strict all-target Clippy and release passed. Source is `9c0117f` plus the v8 implementation (including drive mapping checks, exact ZIP composition and harness formatting normalization). The Windows release executable is **4,277,248 bytes**, SHA-256 `270a0756c3b06dda67fcbc9e2423f496294a1f3e7297a0948f9299eb9c55a6fa`. This is a local validation artifact, not a published release; RSS/CPU and active latency were not measured. A separate Linux-on-WSL full gate passed 392 distinct tests. Later documentation-only edits do not change this binary's recorded source scope.

Native source review found that adding `FILE_OPEN_NO_RECALL` to the NtCreateFile option set returned `STATUS_INVALID_PARAMETER` on this host, while the reviewed directory/reparse/synchronous set succeeded. A separate read-only root-handle reproduction confirmed it. The unsupported flag was removed; no retry, weakened DACL, reparse fallback, privilege enablement or raw diagnostic logging was added to production. Synthetic tests then passed. The schema/visibility harness also normalizes rustfmt's trailing parameter comma without accepting any different function signature.

The subsequent **v7 package observation full gate** passes **355 distinct native Windows GNU tests**, zero ignored, including 54 harness regressions counted once, strict Clippy and release compilation. Source baseline is architecture-only `0521c7cf066958f7cea79817f6a4ca657c5db87e` plus v7 implementation. New tests include actual native OS-entropy generation, all five package suites, synthetic loopback SSH fixed-command capture and existing real-binary tests. This remains host/fixture acceptance, not execution against a router from Windows. The earlier artifacts and timestamps below retain their original scopes; no new RSS/CPU claim is made.

Subsequent passive wireless increment: the same native full gate passed at **2026-09-13T06:30:30Z–06:31:19Z**, with **325 distinct tests**, zero ignored, after adding seven feature and three MCP regressions. Source baseline `979029803271c81386118699b2669e372604ef15` plus the wireless changes; seventeen reads/eight typed contracts. This extends synthetic host acceptance only, not real-radio or emulator evidence. The original 315-test result and artifact below remain historical and are not silently relabeled as this build. See [overall validation](validation.md).

The unmodified `tools/Test-Repository.ps1` native branch passed architecture/evolution checks, all 51 harness regressions, formatting, strict all-target Clippy, workspace tests and release compilation. The recorded evidence run was **2026-09-13T06:08:47Z–06:09:03Z**, following an earlier successful full run. **315 distinct tests** passed with zero ignored; the harness is executed twice by the gate and counted once. Existing Linux-only local-process, protected-file and audit fixtures are not Windows tests. All required portable suites execute on this host, including all five actual-binary stdio cases and eighteen loopback SSH integration cases.

The production baseline is commit `8f6c09587cf34d627522b7da31c97df461e175c0`, plus the test-only environment fix described below. No production code, dependency, architecture contract, CI requirement or repository gate was changed to obtain this result. A separate Linux-on-WSL full gate also passed after the fix; its 347-test inventory is separate, not added to the native count.

The original native tests covered the common environment-configuration/key-source and stderr-audit path, policy, age and bounded ZIP primitives, actual MCP pipes, pinned SSH against synthetic loopback endpoints, capability gating and bounded responses. Those endpoints do not run router commands. Protected files were unsupported in that original result and gain separately scoped v8 evidence above; rotating audit files, system-log delivery and Personal Vault remain unsupported. A GNU run does not establish MSVC/macOS acceptance or execution of the configured GitHub CI jobs.

## Test-environment defect and regression

The first native gate failed two real-binary SSH tests. Their hermetic child launcher used `env_clear()`, which also removed `SystemRoot`. A small native Rust loopback reproduction observed successful TCP connections with the inherited environment, Winsock error **10106** with an empty environment, and success with only `SystemRoot` retained. Microsoft identifies 10106 as [service-provider initialization failure](https://learn.microsoft.com/en-us/windows/win32/winsock/windows-sockets-error-codes-2); the environment dependency is evidence from this host's reproduction, not a claim that the error has only one cause.

The fixture now retains just an existing `SystemRoot`, before setting explicitly supplied synthetic fixture variables. It still clears ambient PATH, configuration and keys. The previously failing actual-binary cases now pass within the full gate; no test was ignored or OS-disabled, and no production SSH fallback was added. The initial failure record was retained separately from successful transcripts.

## Native toolchain and artifact

- Windows 11 Professional x64, build 26200; PowerShell 7.6.6. The Windows API reports version 10.0.26200.
- Rust 1.95.0 (`59807616e1fa2540724bfbac14d7976d7e4a3860`), Cargo 1.95.0 (`f2d3ce0bd7f24a49f8f72d9000448f8838c4e850`), host/target `x86_64-pc-windows-gnu`.
- GCC 15.2.0, POSIX-SEH-MSVCRT, rt_v13-rev1; actual C compilation and linking, including the SSH crypto dependency.
- Release executable: **4,194,304 bytes (4.00 MiB)**; SHA-256 `0ffd87e3f17b3f1e45b4edc9880439e320cdaee2db94276ae0a31ad662cee8fe`. This is a local validation artifact, not a published release; native RSS, CPU and active-work latency were not measured.

The toolchain, caches, outputs and synthetic test scratch space were isolated in an ACL-restricted, non-synced temporary directory outside both repositories. Environment changes applied only to validation child processes; no global Rust installation, system PATH, Visual Studio components or registry settings were modified. Existing Visual Studio lacked the C++/SDK components needed for an MSVC run, so that target remains pending.

Archive sizes and SHA-256 digests were checked before extraction. The six Rust components came from the [official 2026-04-16 distribution](https://static.rust-lang.org/dist/2026-04-16/channel-rust-1.95.0.toml), with filenames `<component>-1.95.0-x86_64-pc-windows-gnu.tar.xz`. GCC came from the [MinGW-w64 toolchain provider's release](https://github.com/niXman/mingw-builds-binaries/releases/tag/15.2.0-rt_v13-rev1), file `x86_64-15.2.0-release-posix-seh-msvcrt-rt_v13-rev1.7z`.

| Component | Archive bytes | SHA-256 |
| --- | ---: | --- |
| rustc | 100389960 | `735c4b5c1459f82a8a1a5251f5899a958c4cf0d92ed3c992691cfff083d22623` |
| cargo | 11744584 | `32afca18643ed650250297614de7e13b2e03eae0ecde6fe250f96b795e6b63e0` |
| rust-std | 26327096 | `f57e045016a04130125fb43295d95f9ad2bebc296150eadb031dbf5167ad12bd` |
| rust-mingw | 5473684 | `ac0b67b7ee8a35045c26b8e39a524f171b2dc4a18838bb15844308e4442802f2` |
| rustfmt | 2910384 | `10adca16ab047fade3d39d594fe0b212e40e1edcea40d9acc8d153e56d2ad111` |
| clippy | 5129080 | `75a28d08651024743d5348e0890b2c0acbc2e7c55f6712394db13f6b8f47b7f9` |
| GCC/MinGW-w64 | 103355261 | `cd21f5b1fd07ca87aa02c6c48537eb8791a441393ca03f1b88ebce53987512c1` |

Toolchain archives total 255,330,049 bytes (243.50 MiB), within the bounded 300 MiB setup allowance. The separate Cargo cache contains 267 downloaded crate archives totaling 29,184,502 bytes; expanded files and build outputs consume additional disk space. These are observed setup sizes, not server runtime requirements.

No real key, Vault archive, router configuration or decrypted backup was used. No remote publication or live-router change occurred. See [platform support](platform-support.md) and [overall validation](validation.md) for remaining acceptance work.
